<!doctype html>
<html lang="en" class="h-full">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title><%= title || "Dashboard" %></title>

  <!-- Tailwind (Play CDN) -->
  <script src="https://cdn.tailwindcss.com"></script>
  <script>
    tailwind.config = {
      theme: {
        extend: {
          colors: {
            slatebg: '#0f172a',  /* background */
            card:    '#0b1220',  /* panel bg */
          }
        }
      },
      darkMode: 'class'
    }
  </script>
  <link rel="stylesheet" href="/styles.css">
</head>
<body class="min-h-full bg-slatebg text-slate-200">
  <header class="border-b border-slate-800/70 bg-[#0b1020]">
    <div class="mx-auto max-w-6xl px-4 py-3 flex items-center justify-between">
      <a href="/dashboard" class="font-semibold tracking-wide">Parazit Bot — Dashboard</a>
      <nav class="text-sm space-x-4">
        <a class="hover:text-white/90" href="/dashboard">Home</a>
        <a class="hover:text-white/90" href="/dashboard/tickets?which=1">Tickets</a>
        <a class="hover:text-white/90" href="/dashboard/kick">Kick Alert</a>
        <a class="hover:text-white/90" href="/logout">Logout</a>
      </nav>
    </div>
  </header>

  <main class="mx-auto max-w-6xl px-4 py-6 grid gap-6">
